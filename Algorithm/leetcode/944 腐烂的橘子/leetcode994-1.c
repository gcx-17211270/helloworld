int orangesRotting(int** grid, int gridSize, int* gridColSize){
    int i = 0;
    while(!isRotted(grid))
    {

        i++;
        if(grid == grid.last) return -1;
    }
    return i;
}
int isRotted(int** grid, int gridSize, int* gridColSize)
{   
    while(grid)
    {

        if(a == 1)return 0;
    }
    return 1;
}
